#include "main.h"
/**
 * _abs -  computes the absolute value of an integer
 *
 * @d: integer to be checked
 * Return: always 0
 */
int _abs(int d)
{
	if (d < 0)
	{ d = (-1) * d;
	}
	return (d);
	return (0);
}
