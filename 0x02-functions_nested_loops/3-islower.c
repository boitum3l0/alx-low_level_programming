#include "main.h"
/**
 * int _islower(int c) - looks for lowercase 
 *
 * Return: Always 0
 */
int _islower(int c)
{
	if (_islower(c))
	{	_putchar(1);
	} else
	{	_putchar(0);
	}
	_putchar('\n');
	return (0);
}
