#include "main.h"
/**
 * int _islower(int c) - looks for lowercase 
 *
 * Return: Always 0
 */
int _islower(int c)
{
	if (_islower(c))
	{	return (1);
	} else
	{	return (0);
	}
	_putchar('\n');
	return (0);
}
